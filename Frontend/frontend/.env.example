# Backend URL — change this to your deployed Vercel backend URL in production
VITE_BACKEND_URL=http://localhost:5000

# Enable Socket.IO (real-time chat) — only set to true when backend is on Railway/Render/VPS
# Socket.IO does NOT work on Vercel serverless (will show repeated 404 errors)
VITE_ENABLE_SOCKET=false
